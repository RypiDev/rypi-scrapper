export enum DomainTypes {
  Portuguese = 'com.br',
  Turkish = 'com.tr',
  English = 'com',
  German = 'de',
  Spanish = 'es',
  Finnish = 'fi',
  French = 'fr',
  Italian = 'it',
  Dutch = 'nl'
}

export const SUPPORTED_LANGS = Object.keys(DomainTypes)
